import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'calculadora.de.perimetro',
  appName: 'calculadora-de-perimetro',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
